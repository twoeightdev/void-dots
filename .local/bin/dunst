#!/bin/sh
# sets dunst notification default color including dunstify
red=$(xgetres color1)
bg=$(xgetres background)
fg=$(xgetres foreground)
/usr/bin/dunst -lf $fg -lb $bg -nf $fg -nb $bg -cf $red -cb $bg "$@"
