#!/bin/sh
# skip alert for dunst scripts
# alsa
#aplay ~/.local/share/audio/beep.wav

if [ $1 = 'mpcnotify' ]; then
    exit 0
elif [ $1 = 'audiobar' ];then
    exit 0
elif [ $1 = 'Signal' ]; then
    exit 0
else
    paplay ~/.local/share/audio/beep.wav
fi
