#!/bin/sh
# skip alert for mpdnotify
if [[ "$1" == "mpdnotify" ]]
then
  exit 0
fi

if [[ "$1" == "volume" ]]
then
  exit 0
fi

if [[ "$1" == "Signal" ]]
then
  exit 0
fi

# alsa
#aplay ~/.local/share/audio/beep.wav

# pulseaudio
paplay ~/.local/share/audio/beep.wav
