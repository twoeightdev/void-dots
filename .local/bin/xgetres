#!/bin/sh
# needed by my dunst script to output xresources colors
if [ $1 = '-colors' ]; then
  xrdb -query | sed -En 's/.*color([0-9][0-9]?)/\1/p' | sort -n | cut -f2
else
  xrdb -query | awk "/$@/ {print \$2; exit}"
fi
